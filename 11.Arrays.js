//code1 using for-of loop
let cities=["lahore","karachi","multan"];
for(let city of cities){
    console.log(city);
}

//code2 using for loop
let heroes=["thor","hulk","spiderman","ironman"];
for(let idx=0; idx<heroes.length; idx++){
    console.log(heroes[idx]);
}